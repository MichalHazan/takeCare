// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    pinkCare: {
      main: '#ffabab',
      light: '#f9caca',
      dark: '#fd9999',
      contrastText: '#604645',
    },
  },
});

export default theme;
